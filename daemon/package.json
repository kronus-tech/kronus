{
  "name": "kronus-daemon",
  "version": "4.2.0",
  "description": "Kronus Telegram control plane daemon — routes Telegram messages to Claude Code sessions per project",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts"
  },
  "dependencies": {
    "grammy": "^1.35.0",
    "strip-ansi": "^7.1.0"
  },
  "devDependencies": {
    "bun-types": "^1.3.11",
    "typescript": "^5.9.3"
  }
}
